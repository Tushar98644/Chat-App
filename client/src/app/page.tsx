import {Layout} from "@/components";
import {SideBar,Chat} from "@/components";

const Home_page = () => {
  return ( 
    <Layout>
      <SideBar/>
      <Chat/>
    </Layout>
   );
}
 
export default Home_page;